
services:
  mysql_database:
    image: mysql:9.0.1
    restart: always
    ports: 
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD
      - MYSQL_DATABASE
    env_file: 
      - path: backend/.env
    volumes:
      - mysql-db:/var/lib/mysql

  backend:
    restart: always
    build:
      dockerfile: dockerfile
      context: backend/
    ports:
      - 8080:8080
    environment:
      - MYSQL_HOST
    env_file: 
      - path: backend/.env
    depends_on:
      - mysql_database
    volumes:
      - backend:/var/lib/backend

volumes:
  mysql-db:
  backend:
